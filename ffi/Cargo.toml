[package]
name = "deno-webmidi-ffi"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
midir = "0.10"
once_cell = "1.20"
parking_lot = "0.12"

[profile.release]
opt-level = 3
lto = true
